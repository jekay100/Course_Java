Properties 属性文件#号是注释，只支持英文编码，不支持GBK，Java提供了命令行工具转化这个
Java经常用做系统配置文件,是一个格式化文件 
key = value的形式，这个文件相当于个map集合
Java提供了一个类，专门用来处理这个文件
命令：native2ascii test.src test.properties ，其中的2表示to,表示本地转化为ascii
load() 解析 
store()相反
list()
getProperty()获得指定属性
setProperty()设置指定属性
+++++++++++++++++++++++++++++++++++++++++++++++++++==
网络编程：分7层协议
 	TCP协议，也是面向对象的对生活的描述
 		服务（Service）：如食堂，商店一直开着的叫host他host在那里等我们
 		WEB服务，为什么叫WEB服务，因为他一直等我们着了，一直开着了相当于个商店 
 	Client - > Service
  1 TCP编程：
 	  食堂卖饭的过程                                                                                 服务器
 	    A 食堂有地址：有8个食堂	食堂1，食堂2，			  A IP,域名标识计算机不同的服务，地点			
 	    B 每个食堂，有提供不同服务类型的窗口				  B 不同的端口（Socket），不同的端口提供不同的服务最大是65535（），前1000用户一般不用，为计算机提供服务的 
 	    C 每个窗口可以为多个同学服务	                       C 每个端口可以建立多个Socket实例，每个实例代表对客户的服务，这里用到了线程
 
 
 
 	    					 
 	  学生买饭的过程                                                                                客户端连接的过程 
 	  	A 找到一个食堂										A. 使用域名找到指定服务器
 	  	B 找到一个窗口										B  使用服务器的端口找到指定服务端口 
 	  	C 请求指定服务（他是提供者你找他，他不找你）			C  建立Socket对象（两个人之间就是平等的，相互交流，服务器有多个socket（一对流（输入输出流）），）
 	  	D 有窗体提供对应的反馈								D  使用流与服务通信
 	   
 	 怎么取消启动服务   
 	 
 	 为什么不同计算机连接不上，对方的电脑，是因为电脑有防火墙
 	 127.0.0.1 = localhost(127开头)，IP协议规定
 	 看一下那个图，当，服务器与客户端连起来时全有socket对象， 都 有个双向流
 客户端最少应该写两个线程	 
 	   