IO是java的一个包，研究的是输入与输出 （Input/Output）
1，在包：java.io
1.File类，代表文件/文件夹，也就 是目录
   a 查看文件/目录的属性(文件的大小，文件的时间，文件的属性可读可写)，相对/绝对路径
     目录：相对目录：是JavaJvm启动的目录，Eclipse中默认的相对目录是：项目文件夹
     
   b 创建文件/目录
   c 访问文件系统（目录系统）ls 
   Java中斜线反正全能用////////\\\是转义的
   