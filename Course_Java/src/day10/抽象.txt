abstract抽象，不具体（美女，银行不具体，支付宝也算银行）

抽象方法，银行存，取，
//软件开发中有什么意义，设计阶段（先是分析，），没有具体过程，
	//图书管理系统，抽象吧，叫写成抽象类，然后，借，还，不知道怎么写，全写成抽象方法，
抽象类不能直接实例话，像美女抽象，所以不能实例话，只能被继承，可以定义变量，一个美女
只有继承了才是具体图形
子类必须实现父类定义的方法，
抽象类可以有具体方法，
全部 是抽象方法的的就是纯抽象方法，就成接口了，银行就是纯抽象，就是接口，
抽象和具体是因人面异的，
抽象类只能被继承（）Java是单继承，接口，一个类可以实现多个接口，（多继承，变像实现多继承）


public static void main(String[] args),Java虚拟机找类里面有main的方法

java(Jvm) -> day01,HelloWorld.main(strs),java虚拟机在包位了，所以main方法只能写成public 
类名访问的，所以写成static java按字符数组找了，参数写错了找不到
加个final没事，仍然能找见了










