接口：全部的方法都是抽象方法，全部的属性都是常量
不能实例化，但可以定义变量 
接口变量可以引用具体实现类的实例
接口只能被实现，一个具体类实现接口，必须使用全部的抽象方法
接口中的方法默认是public abstract
变量一定是公共的常量。默认的
 抽象事物是用功能描述的
 接口之间可以继承
 一个具体类可以实现多个接口（一个概念既是会么也是什么。。。）
 抽象的事物有属性但可以写成方法
 接口提供了实现代码，
 
 Java的数据库标准JDBC  
 动态绑定的原则
 run和stop自行车提供的
 由veicle定义的，接口定义了约定，具体类提供实现，
 接口约定 了具体类实现的行为
 sum规定，所有数据库都 可以用Java读取数据，定义了接口，接口上定义取数据的方法，厂商想支持Java必须实现该方法
 这就是JDBC
 抽象类实现了接口，汽车部分具体，部分抽象
 
 
 
 
 
 
 
 
 
 