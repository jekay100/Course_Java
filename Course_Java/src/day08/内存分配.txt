Point o = new Point(1,2);
栈和堆：
	堆：一堆东西没有先后顺序，哪有空用哪
		　堆里面的东西无序
	栈：是有顺序的，
上面的那个代码先运行new Point(1,2);
先new Goo的时候看看有没有加载结果，.class也是就是对象，如果没有就在classpath里面找，
类只加载一次，发现有继承的时候 全加载进去，按需加载
所有类加载结果就是对象，先分配父类空间，并初始化，然后复会，然后调用构造器
new 是个运算符，邦我们分配堆内存，按类型的属性来分配，里面有，x,y也就是２个３２位
他就在堆里面切了２个int初始化０;并标记为Point,new运算下一步调用构造器,执行构造器之前有了x,y,有了空间
this是当前对象的引用，this就指象标记了的那个Point空间
this在栈里，执行构造之前就有this
Java在栈里弄临时变量

new之后反回的是个地址也叫引用

Math.sqrt();
Math.PI
