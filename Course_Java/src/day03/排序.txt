选择排序
冒泡排序
插入排序第一个默认排序了
效率比上面两种要高 

递归先考虑第n项
1+2+3+4+5+。。。+n = n +f(n -1) && f(1) = 1;
递归涉及到内存管理栈
每一次调用全会产生栈的临时变量
什么时候 能把栈弄满了windows有64M

Java内存管理是透明的是看不见的（空气是透明的所以看不见空气）
递归经常写成循环递归性能比较差因为把内存全分出去了
但是文件目录用的是递归


